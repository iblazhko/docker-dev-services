version: '3'

services:

  mongo:
    image: mongo:4.2.1
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: "root"
      MONGO_INITDB_ROOT_PASSWORD: "BNYpXiaRK7qnL3hmY5pQ"
    env_file:
      - ../.env/mongo-4.2.env
    ports:
      - 27017:27017
    networks:
      - devsvcnet

volumes:

  mongo-data:
    driver: local
